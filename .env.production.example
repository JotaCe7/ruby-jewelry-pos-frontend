# Template only — never committed with real values. The deploy workflow
# writes the actual .env.production file on the VPS from this repo's
# GitHub "production" Environment (Settings > Environments > production).
IMAGE_TAG=
DEPLOY_NETWORK=ruby-prod
HTTPS_PORT=443
